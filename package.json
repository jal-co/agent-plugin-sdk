{
  "name": "agent-plugin-sdk-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "build:sdk": "turbo build --filter=@jal-co/agent-plugin-sdk",
    "test:sdk": "turbo test --filter=@jal-co/agent-plugin-sdk",
    "prepare": "husky"
  },
  "packageManager": "pnpm@10.30.3",
  "devDependencies": {
    "husky": "^9.1.7",
    "turbo": "^2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}
